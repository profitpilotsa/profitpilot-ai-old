import { describe, expect, it } from "vitest";
import { products } from "../server/db/schema";
import { PostgresCommerceRepository } from "../server/repositories/postgresCommerce";
describe("PostgreSQL Product persistence contract",()=>{
 it("defines scoped platform identity and nullable source metadata",()=>{for(const column of [products.organizationId,products.storeId,products.platform,products.externalId])expect(column).toBeDefined();expect(products.sku.notNull).toBe(false);expect(products.sourceUpdatedAt.name).toBe("source_updated_at");expect(products.ingestedAt.name).toBe("ingested_at");});
 it("hydrates a scoped product collection and preserves empty results",async()=>{const rows=[{id:"product-1",organizationId:"org-1",storeId:"store-1",platform:"salla",externalId:"external-1",source:"platform",sourceUpdatedAt:null,ingestedAt:new Date("2026-01-02T00:00:00Z"),name:"Product",sku:null,status:"active",createdAt:new Date("2026-01-01T00:00:00Z")}];const db={select:()=>({from:()=>({where:async()=>rows})})};const repository=new PostgresCommerceRepository(db as never);await expect(repository.listProducts({organizationId:"org-1",storeId:"store-1",mode:"live"},"salla")).resolves.toEqual([{id:"product-1",organizationId:"org-1",storeId:"store-1",mode:"live",externalId:"external-1",source:"platform",name:"Product",sku:undefined,status:"active",createdAt:"2026-01-01T00:00:00.000Z",sourceUpdatedAt:undefined}]);const emptyRepository=new PostgresCommerceRepository({select:()=>({from:()=>({where:async()=>[]})})} as never);await expect(emptyRepository.listProducts({organizationId:"org-1",storeId:"store-1",mode:"live"},"salla")).resolves.toEqual([]);const read=PostgresCommerceRepository.prototype.listProducts.toString();for(const filter of ["eq(products.organizationId,scope.organizationId)","eq(products.storeId,scope.storeId!)","eq(products.platform,platform)"])expect(read).toContain(filter);});
});
